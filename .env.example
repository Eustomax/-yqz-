# ============================================================
#  B站直播机器人 环境变量模板（yqz-liverobot）
# ============================================================
# 用法：复制本文件为 .env 后填入真实值
#   cp .env.example .env
# 说明：
#   - .env 含敏感凭证，已加入 .gitignore，请勿提交到公开仓库。
#   - 代码通过 python-dotenv 自动加载 .env（需 pip install python-dotenv）；
#     若该依赖缺失，仍可用真实环境变量（export / 容器 / systemd 注入）。
#   - 若值留空，程序不会崩溃，但登录态相关功能（弹幕/推送）将不可用。
# ============================================================

# ---------- B站账号凭证（必填，用于登录态）----------
# 获取方式：浏览器登录 https://www.bilibili.com 后，打开开发者工具 -> Application -> Cookies
#   SESSDATA / BUVID3 取自 bilibili.com 的 Cookie
#   bili_jct 取自 bilibili.com 的 Cookie（即 CSRF token）
BILI_SESSDATA=在此填入你的_SESSDATA
BILI_JCT=在此填入你的_bili_jct
BILI_BUVID3=在此填入你的_buvid3

# ---------- 热更新接口鉴权（可选）----------
# 设置后，以下 4 个接口需携带请求头 Authorization: Bearer <token>
#   POST /api/hot_gift
#   POST /api/delete_specific_gift
#   POST /api/send_danmu
#   POST /api/set_status
# 不设置则不校验，兼容仅本机访问的旧部署（不推荐暴露在公网）
BOT_HOT_API_TOKEN=
